<% if target.errors.any? %>
  <h2> <%= pluralize(target.errors.count, "errors") %></h2>
  <ul>
    <% target.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
  </ul>
<% end %>
